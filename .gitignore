node_modules/
.DS_Store
.env
.env.local
.env.*.local

# Build output
dist/
build/

# Dev databases + backups
server/data/config.json
server/data/*.db
server/data/*.db.bak
server/data/*.db-journal
*.sqlite
*.sqlite3

# Server logs
server/server.log
server/*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Test artifacts
.playwright-mcp/
*.png
!client/public/**/*.png
!client/src/**/*.png
!docs/**/*.png

# Tooling temp files
.tmp_*
*_tmp.js
.cache/

# Server uploads (user-generated media — never commit, move to S3/B2 in prod)
server/uploads/*
!server/uploads/.gitkeep

# Editor / IDE
.vscode/
.idea/
*.swp
*.swo

# Claude/AI tooling
.claude/

# Local deploy scripts — contain VPS root password, NEVER commit
.deploy/

# Old review docs from prior agent sessions (work artifacts, not source)
REVIEW-*.md
CLEANUP-AUDIT.md
HANDOFF.md

# Graphify (knowledge graph) outputs — local AI analysis, not source
graphify-out/
